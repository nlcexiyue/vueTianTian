<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiantian.mapper.BookDetailMapper">

   <!--   BookDetail findByBookId(Integer bookId);-->
    <select id="findByBookId" parameterType="Integer" resultType="com.tiantian.entity.BookDetail">
        select * from bookdetails where bookId = #{bookId}
    </select>

</mapper>